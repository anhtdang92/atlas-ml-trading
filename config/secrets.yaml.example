# Brokerage API Credentials (Optional - for live trading)
# Copy this file to secrets.yaml and fill in your actual credentials
# NEVER commit secrets.yaml to git!

# Alpaca (for paper/live trading - optional)
alpaca:
  api_key: "your_alpaca_api_key_here"
  api_secret: "your_alpaca_api_secret_here"
  base_url: "https://paper-api.alpaca.markets"  # Use paper trading URL

# Google Cloud Project
gcp:
  project_id: "your-gcp-project-id"
  bigquery_dataset: "stock_data"
  storage_bucket: "your-model-storage-bucket"

# Notifications (Optional)
notifications:
  email: "your-email@example.com"
  slack_webhook: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL"
